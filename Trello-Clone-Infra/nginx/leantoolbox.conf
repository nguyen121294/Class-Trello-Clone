map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# 1. Manage / Admin App -> manage.leantoolbox.app (Port 8081)
server {
    listen 80;
    listen 443 ssl;
    server_name manage.leantoolbox.app admin.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:8081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

# 2. User App -> app.leantoolbox.app (Port 8080)
server {
    listen 80;
    listen 443 ssl;
    server_name app.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

# 3. Backend API -> api.leantoolbox.app (Port 4000)
server {
    listen 80;
    listen 443 ssl;
    server_name api.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 100m;

    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

# 4. Landing Page -> landing.leantoolbox.app (Port 3000)
server {
    listen 80 default_server;
    listen 443 ssl default_server;
    server_name landing.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

# 5. MinIO S3 Media -> media.leantoolbox.app (Port 9000)
server {
    listen 80;
    listen 443 ssl;
    server_name media.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 500m;

    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 300;
        proxy_request_buffering off;
    }
}

# 6. Docker Portainer -> docker.leantoolbox.app (Port 9002)
server {
    listen 80;
    listen 443 ssl;
    server_name docker.leantoolbox.app;

    ssl_certificate /etc/nginx/ssl/leantoolbox.crt;
    ssl_certificate_key /etc/nginx/ssl/leantoolbox.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:9002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
